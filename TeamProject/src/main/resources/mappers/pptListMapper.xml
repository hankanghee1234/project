<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.team.dao.pptListMapper">

	<insert id="create" parameterType="PPTVO">
		insert into
		tbl_pptDesc(fno,userid,ppt_kind,ppt_title,ppt_desc)
		values(#{fno},#{userid},#{ppt_kind},#{ppt_title},#{ppt_desc})
	</insert>

	<select id="pptUserList" resultType="PPTVO">
		select * from tbl_pptDesc
		where userid in(select userid from tbl_member
		where broad_state = 1)
	</select>

	<update id="update" parameterType="PPTVO">
		update tbl_img set img =
		#{img}, modidate = now() where ino = #{ino}
	</update>

	<delete id="delete" parameterType="PPTVO">
		delete from tbl_img where ino
		= #{ino}
	</delete>

</mapper>